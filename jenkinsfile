pipeline {
    agent any
    stages {
        stage('CI Pipeline') {
            steps {
                sh 'pip install -r requirements.txt'
                sh 'python ml_pipeline.py'
            }
        }
    }
}
