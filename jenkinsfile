pipeline {
    agent any
    tools {
        python 'Python3'  // Auto-installs/uses configured Python
    }
    stages {
        stage('CI Pipeline') {
            steps {
                bat 'pip install -r requirements.txt'
                bat 'python ml_pipeline.py'
            }
        }
    }
}
