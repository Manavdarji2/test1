pipeline {
    agent any
    tools {
      python "Default"
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Run Python Script') {
            steps {
                // Check Python version
                bat 'Python --version'

            }
        }
    }
}
