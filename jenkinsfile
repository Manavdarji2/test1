pipeline {
    agent any
    tools {
        jenkins.plugins.shiningpanda.tools.PythonInstallation Default: ''
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Run Python Script') {
            steps {
                // Check Python version
                bat 'Python --version'

            }
        }
    }
}
